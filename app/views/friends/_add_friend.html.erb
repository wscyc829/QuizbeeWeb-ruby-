<%= form_for :af, url: add_friend_path, autocomplete: :off, remote: true do |f| %>
	<%= f.text_field :first_name, placeholder: "Fisrt name" ,class: 'sometextbox'%>
	<p>
	<%= f.text_field :last_name, placeholder: "Last name",class: 'sometextbox' %>
	<%= f.submit "Add friend", class:'somebuttonred' %>
<% end %>